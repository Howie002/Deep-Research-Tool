{
  "key": "deep-research",
  "name": "Deep Research Agent",
  "command": "cd research-agent && source .venv/bin/activate && python run.py api",
  "port": 8765,
  "health": "http://localhost:8765/health",
  "depends_on": [
    "llm-proxy"
  ],
  "note": "Entry is run.py (nested research-agent/). Do NOT use start.sh (interactive setup wizard). NPM path: /DeepResearch → 10.2.35.10:8765 (rewrite strips prefix; proxy_buffering off; proxy_read_timeout 3600s)."
}