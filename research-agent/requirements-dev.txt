# Development / test dependencies
# Install with: pip install -r requirements-dev.txt
pytest>=8.0
httpx>=0.27          # required by starlette TestClient
pytest-asyncio>=0.23 # async test support (optional, for future async tests)
